import React from "react";

const Login = () => {
  return (
    <div className="bg-green-200">
      <div className="container mx-auto h-screen flex flex-col items-center justify-center">
        <h1 className="text-5xl font-bold">This will be Login</h1>
        <p className="text-center">
          This is the page to implement authentication. Users will see a login
          form and add a link that will redirect the user to the Register page.
          Add at least one social login in both the Join Us and Register pages.
          Implement react-hook-form in the registration & login page.
        </p>
      </div>
    </div>
  );
};

export default Login;
